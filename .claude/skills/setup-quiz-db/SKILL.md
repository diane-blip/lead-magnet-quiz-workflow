# Setup Quiz Database

Automate Supabase database setup for quiz funnels generated by `/lead-magnet-quiz`.

## Trigger

```
/setup-quiz-db [business-name]
```

If no business name provided, list available funnels and ask which one to set up.

---

## Prerequisites

Before running this skill, the user must have:
1. A Supabase project created at [supabase.com](https://supabase.com)
2. The following credentials ready:
   - Project URL (from Settings > API)
   - Service Role Key (from Settings > API)
   - Database URL (from Settings > Database > Connection string)
   - Database password (used when creating the project)

---

## Workflow

### Step 1: Locate the Quiz Funnel

1. If business name provided, construct path:
   ```
   output/[business-name]/
   ```

2. If no business name, list available funnels:
   ```bash
   ls output/
   ```
   Then ask user which one to set up.

3. Find the deploy folder with package.json (handles nested structure):
   ```bash
   find [output-folder] -name "package.json" -path "*/deploy/*" | head -1
   ```
   The folder containing this package.json is where we run commands.

### Step 2: Verify Quiz Funnel Exists

Check that required files exist:
- `package.json` with `setup-db` script
- `.env.example` template
- `scripts/setup-schema.js`
- `supabase/schema.sql`

If any are missing, report error and exit.

### Step 3: Collect Credentials

Ask the user for their Supabase credentials. Present this message:

```
I need your Supabase credentials to set up the database.

**From Supabase Dashboard:**

1. **SUPABASE_URL** (Settings > API > Project URL)
   Example: https://abcdefgh.supabase.co

2. **SUPABASE_SERVICE_ROLE_KEY** (Settings > API > service_role key - the secret one)
   Example: eyJhbGciOiJIUzI1NiIs...

3. **SUPABASE_DB_URL** (Settings > Database > Connection string > URI)
   - Replace `[YOUR-PASSWORD]` with your database password
   Example: postgresql://postgres:mypassword@db.abcdefgh.supabase.co:5432/postgres

**Configuration:**

4. **TABLE_PREFIX** (default: [initials]_)
   This prefixes all tables: [prefix]leads, [prefix]quiz_responses, etc.

5. **ADMIN_PASSWORD** (for /admin analytics dashboard)
   Choose a secure password.
   **IMPORTANT**: Avoid special characters like `!` `$` `\` `` ` `` - they cause shell escaping issues with Vercel CLI.
   Use alphanumeric characters and simple symbols (`-` `_` `.`) only.

**Optional:**

6. **GUMLOOP_WEBHOOK_URL** (for automation triggers)
   Leave blank if not using Gumloop.

7. **RESEND_API_KEY** (for email sending)
   Leave blank to skip email functionality.

8. **DEAL_VALUE** (average deal value or order value, for ROI tracking)
   Example: 3000 (for a $3,000 coaching package) or 75 (for a $75 average order)
   Leave blank to hide the Revenue Impact section on the admin dashboard.

9. **CLOSE_RATE** (estimated close rate as a decimal, for ROI tracking)
   Example: 0.15 (15% of hot leads convert to customers)
   Leave blank to hide the Revenue Impact section on the admin dashboard.

10. **DATA_RETENTION_ANALYTICS_DAYS** (default: 90)
    How many days to keep analytics events before automatic cleanup.
    Leave blank for default of 90 days.

11. **DATA_RETENTION_EMAIL_DAYS** (default: 365)
    How many days to keep sent email log records before automatic cleanup.
    Leave blank for default of 365 days.

Please provide these values (you can paste them all at once or one by one):
```

### Step 4: Create .env File

After collecting credentials, create the `.env` file in the deploy folder:

```bash
# Write .env file with collected values
```

Content template:
```
# [Business Name] Quiz Funnel - Environment Variables
# Generated by /setup-quiz-db on [date]

# Supabase Configuration (Required)
SUPABASE_URL=[value]
SUPABASE_SERVICE_ROLE_KEY=[value]
SUPABASE_DB_URL=[value]

# Table Prefix (Required)
TABLE_PREFIX=[value]

# Admin Dashboard
ADMIN_PASSWORD=[value]

# Automation Webhook (Optional)
GUMLOOP_WEBHOOK_URL=[value]

# Email Configuration (Optional)
RESEND_API_KEY=[value]

# ROI Tracking (Optional)
DEAL_VALUE=[value]
CLOSE_RATE=[value]

# Data Retention (Optional - defaults shown)
DATA_RETENTION_ANALYTICS_DAYS=90
DATA_RETENTION_EMAIL_DAYS=365
```

### Step 5: Install Dependencies

```bash
cd [deploy-folder] && npm install
```

Report progress and any errors.

### Step 6: Run Database Setup

```bash
npm run setup-db
```

This should output:
```
Connecting to database...
Creating tables with prefix: [prefix]
  - Created [prefix]leads
  - Created [prefix]quiz_responses
  - Created [prefix]email_log
  - Created [prefix]email_templates
  - Created [prefix]content_blocks
  - Created [prefix]analytics_events
Seeding email templates...
  - Seeded 26 email templates
Seeding content blocks...
  - Seeded N content blocks
Database setup complete!
```

If there are errors, report them clearly.

### Step 7: Verify Setup

1. Confirm tables were created (check script output)
2. Report success with next steps

### Step 8: Report Success

Output this summary:

```
✅ Database setup complete!

**Created Tables:**
- [prefix]leads
- [prefix]quiz_responses
- [prefix]email_log
- [prefix]email_templates (26 templates seeded)
- [prefix]content_blocks (60-85 content blocks seeded)
- [prefix]analytics_events

**Next Steps for Vercel Deployment:**

1. Build the project:
   cd [deploy-folder] && npm run build

2. Deploy to Vercel:
   vercel --prod

3. Add environment variables in Vercel Dashboard (Settings > Environment Variables):
   - SUPABASE_URL
   - SUPABASE_SERVICE_ROLE_KEY
   - TABLE_PREFIX
   - ADMIN_PASSWORD
   [- GUMLOOP_WEBHOOK_URL (if configured)]
   [- RESEND_API_KEY (if configured)]
   [- DEAL_VALUE (if configured — enables Revenue Impact on /admin)]
   [- CLOSE_RATE (if configured — enables Revenue Impact on /admin)]
   [- DATA_RETENTION_ANALYTICS_DAYS (optional, default 90)]
   [- DATA_RETENTION_EMAIL_DAYS (optional, default 365)]

4. **CRITICAL: Redeploy after adding env vars:**
   vercel --prod

5. Test your quiz:
   - Visit [your-domain]/quiz
   - Complete the quiz
   - Check /admin dashboard

**Vercel CLI Alternative:**
You can also add env vars via CLI before deploying:
```bash
vercel env add SUPABASE_URL production
vercel env add SUPABASE_SERVICE_ROLE_KEY production
vercel env add TABLE_PREFIX production
vercel env add ADMIN_PASSWORD production
vercel --prod
```
```

---

## Error Handling

### Quiz Funnel Not Found
```
❌ Quiz funnel "[name]" not found.

Available funnels:
- [list funnels]

Usage: /setup-quiz-db [business-name]
```

### Missing Files
```
❌ Missing required files in deploy folder:
- [list missing files]

This quiz funnel may have been generated with an older version.
Please regenerate using /lead-magnet-quiz.
```

### Database Connection Error
```
❌ Database connection failed.

Check your SUPABASE_DB_URL:
- Is the password correct?
- Did you replace [YOUR-PASSWORD] with your actual password?
- Is the Supabase project active (not paused)?

Error: [actual error message]
```

### Tables Already Exist
```
⚠️ Some tables already exist in the database.

To reset and recreate, run this SQL in Supabase SQL Editor:
DROP TABLE IF EXISTS [prefix]analytics_events;
DROP TABLE IF EXISTS [prefix]email_log;
DROP TABLE IF EXISTS [prefix]email_templates;
DROP TABLE IF EXISTS [prefix]content_blocks;
DROP TABLE IF EXISTS [prefix]quiz_responses;
DROP TABLE IF EXISTS [prefix]leads;

Then run /setup-quiz-db again.
```

---

## Notes

- This skill does NOT create the Supabase project - user must do that first
- This skill does NOT deploy to Vercel - it just sets up the database
- Credentials are stored in `.env` which is gitignored
- The nested `deploy/deploy/` structure is intentional for Vercel's root directory detection
